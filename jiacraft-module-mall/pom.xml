<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <artifactId>jiacraft</artifactId>
        <groupId>site.hansi</groupId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>

    <artifactId>jiacraft-module-mall</artifactId>
    <packaging>pom</packaging>

    <name>${project.artifactId}</name>

    <description>
        商城大模块，由 product 商品、promotion 营销、trade 交易、statistics 统计等组成
    </description>
    <modules>
        <module>jiacraft-module-product</module>
        <module>jiacraft-module-promotion</module>
        <module>jiacraft-module-trade</module>
        <module>jiacraft-module-statistics</module>
        <!--
            特殊：为什么会有 jiacraft-module-trade-api 呢？
                jiacraft-module-promotion 和 jiacraft-module-trade 之间相互循环依赖，所以抽出 jiacraft-module-trade-api 模块，这样：
                1. jiacraft-module-promotion 依赖 jiacraft-module-trade-api
                2. jiacraft-module-trade 依赖 jiacraft-module-promotion
            从而不存在相互（循环）依赖，即 jiacraft-module-trade => jiacraft-module-promotion => jiacraft-module-trade-api
         -->
        <module>jiacraft-module-trade-api</module>
    </modules>

</project>
